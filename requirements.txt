# Combined requirements for Jenkins Python AI agent
# Includes dependencies for both ingestion scripts and RAG service

# Ingestion script dependencies
google-cloud-aiplatform>=1.38.0,<2.0.0
tiktoken>=0.5.0
chromadb>=0.4.0

# RAG service dependencies (Pinned to verified versions)
fastapi==0.121.3
uvicorn[standard]==0.38.0
python-dotenv>=1.0.0
pydantic==2.12.4
slowapi>=0.1.9

# Additional dependencies
importlib-metadata>=6.0.0
vertexai

# For the Vordu project (Pinned)
pytest==9.0.1
pytest-bdd==8.1.0
playwright==1.40.0
requests==2.32.5
sqlalchemy==2.0.44
